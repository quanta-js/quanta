---
'@quantajs/vue': minor
---

New package: Vue bindings. `useQuantaValue` returns a ref that updates only when the state its selector reads changes; `useQuanta`, `useQuantaActions` and `useLocalStore` match their React counterparts; `createQuanta()` gives an app its own container and hydrates a server snapshot.

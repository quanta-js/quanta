---
'@quantajs/react': major
'@quantajs/devtools': major
---

`@quantajs/react` now requires `@quantajs/devtools` from the same major (`^3.0.0`), and `@quantajs/devtools` requires `@quantajs/core` from the same major, so 2.x and 3.x packages can no longer be installed together without a warning.
